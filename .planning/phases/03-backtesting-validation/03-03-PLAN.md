---
phase: 03-backtesting-validation
plan: 03
type: execute
wave: 3
depends_on: [03-02-PLAN]
files_modified:
  - .planning/phases/03-backtesting-validation/results/2025_backtest_report.html
  - .planning/phases/03-backtesting-validation/results/2025_journal_export.csv
  - .planning/phases/03-backtesting-validation/results/2025_equity_curve.csv
autonomous: true
requirements:
  - REQ-001
  - REQ-002
  - REQ-003
  - REQ-004
  - REQ-005
  - REQ-006
  - REQ-007
  - REQ-008
  - REQ-009
  - REQ-010
  - REQ-011
  - REQ-012
  - REQ-013
  - REQ-014
  - REQ-015
  - REQ-016
  - REQ-017
  - REQ-018
  - REQ-019
  - REQ-020
  - REQ-021
  - REQ-022
  - REQ-023
  - REQ-024
  - REQ-025
  - REQ-026
  - REQ-027
  - REQ-028
  - REQ-029
  - REQ-030
  - REQ-031
  - REQ-032
  - REQ-033
  - REQ-034
  - REQ-035
  - REQ-036
  - REQ-037
  - REQ-038
  - REQ-039
  - REQ-040
  - REQ-041
  - REQ-042

must_haves:
  truths:
    - "2025 backtest completes on MT5 Strategy Tester with Every Tick mode (sequential execution after Plan 02 completes)"
    - "Backtest journal exports to CSV with all trade details (entry, exit, setup type, P&L, slippage)"
    - "Equity curve data captured (date, equity value) for daily drawdown calculation"
    - "History quality indicator ≥95% (goal >99%)"
    - "No critical errors during backtest; trades executed and logged across different market regime than 2024"
  artifacts:
    - path: .planning/phases/03-backtesting-validation/results/2025_backtest_report.html
      provides: "MT5 Strategy Tester native report output (metrics summary, equity curve, test details)"
      contains: "2025, History Quality, Total Trades, Win Rate, Profit Factor"
    - path: .planning/phases/03-backtesting-validation/results/2025_journal_export.csv
      provides: "MT5 Journal export (trade-by-trade: entry time, price, setup type, exit, P&L, slippage)"
      contains: "Time, Symbol, Direction, EntryPrice, LotSize, SetupType, ExitTime, P&L_Currency"
    - path: .planning/phases/03-backtesting-validation/results/2025_equity_curve.csv
      provides: "Daily equity data (for drawdown calculation)"
      contains: "Date, Equity, Daily_Profit, Cumulative_Loss"
  key_links:
    - from: "Phase 2 EA (.ex5 binary)"
      to: "MT5 Strategy Tester"
      via: "backtest execution"
      pattern: "EA loads in tester, runs without crashes for full 252 trading days of 2025"
    - from: "MT5 backtest journal"
      to: "2025_journal_export.csv"
      via: "export"
      pattern: "All trades logged with setup_type, P&L, timestamps"
    - from: "2025_journal_export.csv"
      to: "validation_checklist.md (Task 5)"
      via: "metrics calculation"
      pattern: "CSV parsed, trade counts extracted, gates verified for regime robustness"

---

<objective>
Execute full-year 2025 backtest on MT5 Strategy Tester using Every Tick mode and real tick data. Validate that the EA performs consistently across a different market regime (2025 vs. 2024). This second backtest is critical to prove the EA is not over-optimized to 2024 market conditions. This plan runs sequentially after Plan 02 completes — MT5 Strategy Tester is single-instance and supports only one backtest execution at a time.

**Purpose:** Generate quantitative proof that EA rules work across TWO DISTINCT market regimes. If 2024 passes but 2025 fails (or vice versa), EA has regime-dependent bugs that must be fixed in Phase 2 before proceeding to Phase 4. Validate all 42 requirements across different market conditions.

**Output:**
- MT5 backtest report (2025 metrics)
- Journal export CSV (trade-by-trade audit for 2025, validating all 42 requirements)
- Equity curve data (for daily drawdown validation)
- Ready for comparison against 2024 results in Wave 4
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-backtesting-validation/03-CONTEXT.md (D-07 Regime Robustness, D-08 P&L Variance, Failed Gate Protocol)
@.planning/phases/03-backtesting-validation/03-RESEARCH.md (Pitfall 2: Regime-Independent Code Bugs, Pitfall 6: Overfitting Without Out-of-Sample)
@.planning/phases/03-backtesting-validation/backtest_config/2025_settings.json
@.planning/phases/02-signal-detection-execution/02-CONTEXT.md
@.planning/REQUIREMENTS.md (All 42 v1 requirements)

**Why 2025 Matters (from 03-CONTEXT.md D-07):**
- 2024 was consolidating/tight market regime
- 2025 saw major rallies (geopolitical events, XAUUSD reached $3500+)
- If EA works in 2024 but fails in 2025 (or vice versa), indicates regime-dependent weakness
- BOTH years must independently meet gates to prove robustness

**MT5 Backtest Settings (identical to 2024, except dates):**
- Symbol: XAUUSD, EURUSD (combined)
- Period: M5 (5-minute)
- Model: Every Tick (real tick data preferred)
- Start Date: 2025-01-01 00:00:00
- End Date: 2025-12-31 23:59:59
- Starting Balance: $1,000 (same as 2024 for consistency)
- Spreads: 3 pips XAUUSD, 5 pips EURUSD

**Success Gates for 2025 (same as 2024, must both pass):**
- Win Rate ≥50%
- Profit Factor ≥1.5
- Max Daily Drawdown ≤2%
- 200+ total trades
- ≥50 Setup 1 AND ≥50 Setup 2 trades
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure MT5 Strategy Tester for 2025 Backtest</name>
  <files></files>
  <read_first>
    .planning/phases/03-backtesting-validation/backtest_config/2025_settings.json
    .planning/phases/03-backtesting-validation/03-RESEARCH.md (Pattern 1)
    .planning/phases/03-backtesting-validation/03-CONTEXT.md (D-07 Regime Robustness)
  </read_first>
  <action>
    **IMPORTANT: Sequential execution after Plan 02 completes. MT5 Strategy Tester is single-instance — run only after 2024 backtest finishes and outputs saved.**
    
    Identical configuration to Task 1 of Plan 02, except dates are 2025:
    
    1. **Launch MT5 Platform**
    
    2. **Open Strategy Tester:**
       - Menu: View → Strategy Tester (or Ctrl+R)
    
    3. **Configure Backtest Settings:**
       - **Expert Advisor:** Select Phase 2 compiled EA (.ex5)
       - **Symbol:** XAUUSD (and add EURUSD if possible in single run, or plan sequential/parallel)
       - **Timeframe:** M5 (5 Minutes)
       - **Model:** Every Tick (real tick data — CRITICAL, same as 2024)
       - **Start Date:** 2025-01-01 00:00:00
       - **End Date:** 2025-12-31 23:59:59 (full year of 2025 market)
       - **Starting Balance:** 1000 USD (consistent with 2024)
       - **Deposit Currency:** USD
    
    4. **Verify All Settings Match 2024 Config (except dates):**
       - Compare to 03-CONTEXT.md backtest_config/2024_settings.json
       - Same symbols, timeframe, model, balance
       - Different dates: 2025 instead of 2024
       - Goal: Identical backtest conditions across regimes
    
    5. **Notes on 2025 Market Context:**
       - 2025 expected to have different regime than 2024 (per research)
       - XAUUSD likely stronger (geopolitical demand)
       - More volatile market environment
       - EA must work in BOTH tight AND volatile regimes to be robust
       - Results may differ from 2024 (expected), but gates must still be met
  </action>
  <verify>
    MT5 tester configured: All 2025 settings locked (dates, symbols, model, balance match checklist)
    Settings ready: Strategy Tester shows 2025-01-01 start date and 2025-12-31 end date
  </verify>
  <done>
    MT5 Strategy Tester configured for 2025 backtest. Settings identical to 2024 except dates. Ready to execute backtest on 2025 market regime data. Sequential execution after Plan 02 completes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute 2025 Backtest and Monitor Completion</name>
  <files>
    .planning/phases/03-backtesting-validation/results/2025_backtest_report.html
    .planning/phases/03-backtesting-validation/results/2025_journal_export.csv
    .planning/phases/03-backtesting-validation/results/2025_equity_curve.csv
  </files>
  <read_first>
    .planning/phases/03-backtesting-validation/03-CONTEXT.md (D-07 Failed Gate Handling if 2025 breaks)
    .planning/phases/03-backtesting-validation/03-RESEARCH.md (Pitfall 2: Regime-Independent Code Bugs)
  </read_first>
  <action>
    **SEQUENTIAL EXECUTION: Start this task only after Plan 02 (2024 backtest) completes and outputs are saved.**
    
    Identical to Task 2 of Plan 02, but for 2025:
    
    1. **Click "Start" in MT5 Strategy Tester**
       - Backtest begins processing 2025 tick data (Jan 1 – Dec 31)
       - Progress bar shows % complete
       - Expected duration: 30 min – 2 hours
    
    2. **Monitor backtest progress:**
       - Watch for errors/crashes (should be rare if Phase 2 is solid)
       - Monitor ticks processed (should be >0 and increasing)
       - If backtest hangs >5 min on single bar, stop and investigate
    
    3. **Upon completion:**
       - Tester shows "Testing complete"
       - Results summary displayed
    
    4. **Save Backtest Report:**
       - Right-click result in tester history
       - Select "Save report"
       - Save as: `.planning/phases/03-backtesting-validation/results/2025_backtest_report.html`
       - Verify file contains: total trades, WR, PF, max DD, history quality %
    
    5. **Export Journal to CSV:**
       - Tools → Journal
       - Filter: 2025-01-01 to 2025-12-31
       - Right-click → "Export to CSV"
       - Save as: `.planning/phases/03-backtesting-validation/results/2025_journal_export.csv`
       - Test load:
         ```bash
         python3 -c "import pandas as pd; df = pd.read_csv('.planning/phases/03-backtesting-validation/results/2025_journal_export.csv'); print(f'Loaded {len(df)} trades')"
         ```
    
    6. **Capture Equity Curve Data:**
       - Extract daily equity from backtest report HTML OR
       - Reconstruct from journal (entry/exit P&L per day → cumulative equity)
       - Save as: `.planning/phases/03-backtesting-validation/results/2025_equity_curve.csv`
       - Format: Date, Equity (252+ daily entries for 2025)
    
    7. **Immediate Sanity Checks:**
       - Total trades: Should be >100 (expecting ~200+)
       - History quality %: Should be >95%
       - Journal CSV: No data corruption (loadable by pandas)
       - Equity curve: Shows activity (not flat)
    
    8. **If 2025 Backtest Fails or Produces Drastically Different Results:**
       - **If <50% win rate in 2025 but >50% in 2024:** Regime-dependent bug in Phase 2. Investigate entry/exit logic.
       - **If Setup 1 count drops 60% in 2025:** Balanced market detection may be broken under 2025 VA width conditions.
       - **If Setup 2 count drops 60% in 2025:** Imbalanced market conditions (likely in 2025) not triggering Setup 2 correctly.
       - Document gap and return findings to Phase 2 for diagnosis.
  </action>
  <verify>
    Files created: `ls .planning/phases/03-backtesting-validation/results/ | grep 2025` shows all three files (report, journal, equity)
    Report valid: `grep -i "total trades" .planning/phases/03-backtesting-validation/results/2025_backtest_report.html` returns metrics
    Journal valid: `python3 -c "import pandas as pd; print(len(pd.read_csv('.planning/phases/03-backtesting-validation/results/2025_journal_export.csv')))"` shows trade count
    Equity curve valid: `wc -l .planning/phases/03-backtesting-validation/results/2025_equity_curve.csv` shows >250 lines
  </verify>
  <done>
    2025 backtest executed successfully (sequential after Plan 02 completes). All outputs captured (report, journal CSV, equity curve). Data ready for metrics calculation and regime comparison against 2024.
  </done>
</task>

<task type="auto">
  <name>Task 3: Quick Data Quality Check on 2025 Results</name>
  <files></files>
  <read_first>
    .planning/phases/03-backtesting-validation/03-RESEARCH.md (Pitfall 2: Regime-Independent Code Bugs, Data Accuracy Spot-Check)
  </read_first>
  <action>
    **Identical sanity checks as Plan 02 Task 3, applied to 2025 results:**
    
    1. **Verify 2025 journal CSV loads and has valid structure:**
       ```bash
       python3 -c "
       import pandas as pd
       import numpy as np
       df = pd.read_csv('.planning/phases/03-backtesting-validation/results/2025_journal_export.csv')
       assert len(df) > 100, f'Only {len(df)} trades (expected >100)'
       print(f'✓ CSV loads: {len(df)} trades')
       missing_pct = (df.isnull().sum().sum() / (len(df) * len(df.columns))) * 100
       assert missing_pct < 5, f'Too many missing values: {missing_pct}%'
       print(f'✓ Missing values: {missing_pct:.1f}% (acceptable <5%)')
       assert 'SetupType' in df.columns
       setup_types = df['SetupType'].unique()
       assert all(st in ['Setup 1', 'Setup 2'] for st in setup_types if pd.notna(st)), f'Invalid setup types: {setup_types}'
       print(f'✓ SetupType format valid: {setup_types}')
       "
       ```
       Expected: All assertions pass, 0 errors
    
    2. **Check for bad data (negative prices, unrealistic P&L, oversized lots):**
       ```bash
       python3 -c "
       import pandas as pd
       df = pd.read_csv('.planning/phases/03-backtesting-validation/results/2025_journal_export.csv')
       
       bad_prices = len(df[(df['EntryPrice'] < 0) | (df['ExitPrice'] < 0)])
       assert bad_prices == 0, f'Found {bad_prices} trades with negative prices'
       print(f'✓ No negative prices: {bad_prices} (expected 0)')
       
       bad_pnl = len(df[df['P&L_Currency'].abs() > 5000])
       assert bad_pnl == 0, f'Found {bad_pnl} trades with P&L > ±5000'
       print(f'✓ No suspiciously large P&L: {bad_pnl} (expected 0)')
       
       bad_lots = len(df[df['LotSize'] > 100])
       assert bad_lots == 0, f'Found {bad_lots} trades with LotSize > 100'
       print(f'✓ No suspiciously large lot sizes: {bad_lots} (expected 0)')
       "
       ```
       Expected: All assertions pass (0 bad records)
    
    3. **Verify all required files exist:**
       ```bash
       test -f .planning/phases/03-backtesting-validation/results/2025_journal_export.csv && echo "✓ Journal CSV exists" || echo "✗ Journal missing"
       test -f .planning/phases/03-backtesting-validation/results/2025_equity_curve.csv && echo "✓ Equity curve file exists" || echo "✗ Equity curve missing"
       test -f .planning/phases/03-backtesting-validation/results/2025_backtest_report.html && echo "✓ Backtest report exists" || echo "✗ Report missing"
       ```
  </action>
  <verify>
    <automated>
    python3 -c "import pandas as pd; df = pd.read_csv('.planning/phases/03-backtesting-validation/results/2025_journal_export.csv'); assert len(df) > 100; missing_pct = (df.isnull().sum().sum() / (len(df) * len(df.columns))) * 100; assert missing_pct < 5; bad_prices = len(df[(df['EntryPrice'] < 0) | (df['ExitPrice'] < 0)]); assert bad_prices == 0; bad_pnl = len(df[df['P&L_Currency'].abs() > 5000]); assert bad_pnl == 0; bad_lots = len(df[df['LotSize'] > 100]); assert bad_lots == 0; print('✓ All sanity checks passed')"
    </automated>
  </verify>
  <done>
    2025 backtest data validated as reasonable. CSV loads without corruption, setup_type field properly formatted (Setup 1/Setup 2), prices/P&L/lot sizes within expected ranges, no missing data exceeding 5%. All required files exist and contain expected data. Ready for Wave 4 metrics calculation and gate validation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 2025 Tick Data → Backtest | Tick data for 2025 must be complete; gaps cause unreliable backtest |
| Phase 2 EA → Different Market Regime | EA must work across both 2024 (tight) and 2025 (volatile) regimes without regime-specific bugs |
| Journal Export Formatting | CSV export may vary between 2024 and 2025 if MT5 updates or broker changes journal format; handled by pandas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | 2025 tick data manipulated or incomplete | Mitigate | History quality % >95% validated in backtest report; spot-check accuracy |
| T-03-08 | Repudiation | Claim "EA works in 2025" when it fails due to regime-dependent bug | Mitigate | Gate validation in Wave 4 will catch any regime-dependent failures (WR drops below 50%, etc.) |
| T-03-09 | Information Disclosure | Journal reveals trade details (P&L, entry prices) | Accept | Backtest is historical analysis; no real trading, no PII involved |

</threat_model>

<verification>
After Task 3 completes:

1. **2025 Backtest Report Generated:**
   - `ls .planning/phases/03-backtesting-validation/results/2025_backtest_report.html`
   - Contains metrics for 2025 year

2. **Journal Export CSV Valid:**
   - `wc -l .planning/phases/03-backtesting-validation/results/2025_journal_export.csv` shows 100+ rows
   - Python loads successfully: `python3 -c "import pandas; pandas.read_csv('.planning/phases/03-backtesting-validation/results/2025_journal_export.csv')"`

3. **Equity Curve Ready:**
   - `ls .planning/phases/03-backtesting-validation/results/2025_equity_curve.csv`
   - 252+ daily entries

4. **Data Quality Checked:**
   - All sanity check assertions pass (no bad data, valid formatting)

**Status:** Wave 3 (2025 backtest) complete. Both 2024 and 2025 journals ready for Wave 4 analysis. Regime comparison can now proceed.
</verification>

<success_criteria>
Wave 3 (2025 Backtest) Complete:

1. ✓ MT5 Strategy Tester configured with Every Tick mode, 2025 date range, $1,000 balance
2. ✓ 2025 backtest executes sequentially after Plan 02 (single-instance MT5 limitation)
3. ✓ Backtest executes successfully across full year (252 trading days)
4. ✓ Backtest report generated with 2025 metrics
5. ✓ Journal CSV exported with 2025 trade-by-trade details (all 42 requirements validated in different regime)
6. ✓ Equity curve data captured for 2025 daily drawdown
7. ✓ Data quality sanity checks passed (no corruption, valid setup_type, reasonable prices/P&L)
8. ✓ History quality >95% (goal >99%)

**Deliverables ready for Wave 4 (comparison analysis, gate validation across both market regimes).**

Both 2024 and 2025 backtests now complete. Ready to calculate metrics and compare regimes.
</success_criteria>

<output>
After 2025 backtest completion, create: `.planning/phases/03-backtesting-validation/03-PLAN-SUMMARY.md`

```markdown
# Phase 3 Plan 03 — 2025 Backtest Execution

**Completed:** [Date]  
**Backtest Period:** 2025-01-01 through 2025-12-31  
**Duration:** [X hours]
**Execution Mode:** Sequential (after Plan 02 completes)

## Metrics from 2025 Backtest Report

**From MT5 Report (raw numbers):**
- Total Trades: [N] (gate: ≥200)
- History Quality: [X%] (target: >99%, minimum: >95%)
- Max Drawdown: [X%]

**To be calculated in Wave 4:**
- Win Rate: TBD
- Profit Factor: TBD
- Setup 1 Count: TBD
- Setup 2 Count: TBD
- Daily Drawdown Max: TBD

## Deliverables

✓ 2025_backtest_report.html — MT5 native report
✓ 2025_journal_export.csv — Trade-by-trade audit (ready for parsing)
✓ 2025_equity_curve.csv — Daily equity (for DD calculation)

## Regime Comparison (2024 vs 2025)

**Will be completed in Wave 4:**
- Trade count 2024: [N], 2025: [M] (variance: [X%])
- If variance >50%: Investigate regime-dependent behavior
- Win rate, profit factor to be compared for robustness signals

## Next: Wave 4

Parse both 2024 and 2025 journals, calculate metrics, validate gates independently, compare regimes.
```
</output>
