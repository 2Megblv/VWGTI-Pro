---
status: ready
phase: 02.1
phase_name: signal-detection-execution-gap-closure-ea-compilation
phase_slug: signal-detection-execution-gap-closure-ea-compilation
type: gap_closure
gap_closure: true
autonomous: true
wave: 1
objective: Fix critical EA compilation errors by creating missing header files and resolving syntax issues. Phase 03 (backtesting) is blocked until VolumeProfile_EA_v1.0.mq5 compiles successfully to .ex5 binary.
blocker_from: 03-01
blocker_phase: 03
blocker_description: Cannot execute backtests without compiled EA binary. MT5 compiler reports 102 errors from missing headers (Utils.mqh, VolumeProfile.mqh, RiskManager.mqh, etc.) and syntax errors.
files_modified:
  - src/VolumeProfile_EA_v1.0.mq5
  - src/Include/Utils.mqh
  - src/Include/VolumeProfile.mqh
  - src/Include/RiskManager.mqh
  - src/Include/SignalDetection.mqh
  - src/Include/MultiTimefameContext.mqh
  - src/Include/TradeExecution.mqh
  - src/Include/RiskLimits.mqh
  - src/Include/JournalLogger.mqh
  - src/Include/ReversalExit.mqh
---

## Objective

Fix critical EA compilation errors by:
1. Creating all 9 missing header files with proper structure
2. Resolving syntax errors in VolumeProfile_EA_v1.0.mq5
3. Verifying successful compilation to .ex5 binary
4. Unblocking Phase 03 backtesting execution

## Context

Phase 03 (backtesting-validation) reached Plan 03-02 (2024 backtest execution) and hit a critical blocker: the Phase 2 EA cannot be compiled. MT5 compiler reports:
- 9 missing header files
- 102 compilation errors
- 4 warnings

**Error Categories:**
- `file 'path/Utils.mqh' not found` — 9 missing header includes
- `'VolumeProfile' - unexpected token, probably type is missing?` — Type definitions missing
- `undeclared identifier 'EA_MAGIC_NUMBER'` — Constants not defined
- `undeclared identifier 'IsConnected'` — Function references not resolved

## Tasks

### Task 1: Analyze Compilation Errors and Map Dependencies
**Objective:** Understand which headers are needed and what they should contain.

**Steps:**
1. Open `src/VolumeProfile_EA_v1.0.mq5` and identify all `#include` statements
2. For each missing header, determine its purpose from how it's used in the main EA:
   - `Utils.mqh` — Utility functions (IsConnected, logging)
   - `VolumeProfile.mqh` — Volume profile data structures and calculations
   - `RiskManager.mqh` — Position sizing and risk calculations
   - `SignalDetection.mqh` — Setup 1/Setup 2 signal detection logic
   - `MultiTimefameContext.mqh` — Multi-timeframe analysis
   - `TradeExecution.mqh` — Trade execution functions
   - `RiskLimits.mqh` — Daily limits enforcement
   - `JournalLogger.mqh` — Trade journal logging
   - `ReversalExit.mqh` — Reversal exit logic
3. Create a dependency map showing which functions/types each header needs to export
4. Commit: `fix: analyze EA compilation errors and map dependencies`

### Task 2: Create Missing Header Files
**Objective:** Create all 9 header files with proper structure and required function/type definitions.

**Steps:**
1. Create `src/Include/Utils.mqh`:
   - Function: `bool IsConnected()` — check broker connection
   - Function: `void LogAlert(string msg)` — alert logging
   - Function: `void LogError(string msg)` — error logging
   - Function: `void LogAudit(string msg)` — audit logging

2. Create `src/Include/VolumeProfile.mqh`:
   - Struct: `VolumeProfile` — POC, VAH, VAL, volume distribution
   - Function: `void CalculateCurrentVolumeProfile(...)` — VP calculation on current bars
   - Function: `void CalculateValueArea(...)` — VA identification

3. Create `src/Include/RiskManager.mqh`:
   - Function: `double CalculateLotSize(...)` — position sizing
   - Function: `double GetMaxDrawdown()` — portfolio drawdown

4. Create `src/Include/SignalDetection.mqh`:
   - Function: `struct Setup1Signal Setup1Signal(...)` — Setup 1 detection
   - Function: `struct Setup2Signal Setup2Signal(...)` — Setup 2 detection
   - Function: `bool DetectSetup1Signal(...)` — Setup 1 boolean check
   - Function: `bool DetectSetup2Signal(...)` — Setup 2 boolean check

5. Create `src/Include/MultiTimefameContext.mqh`:
   - Function: `void Load15MProfile(...)` — Load 15M context
   - Function: `bool IsSessionAllowed(...)` — Session time checks

6. Create `src/Include/TradeExecution.mqh`:
   - Function: `void ExecuteTrade(...)` — Place trade order
   - Function: `void ModifyTrade(...)` — Modify position
   - Function: `void CloseTrade(...)` — Close position

7. Create `src/Include/RiskLimits.mqh`:
   - Struct: `DailyLimitState` — Daily P&L, trade count, symbol tracking
   - Function: `void EnforceDailyLimits(...)` — Enforce daily limits

8. Create `src/Include/JournalLogger.mqh`:
   - Function: `void LogTrade(...)` — Log executed trade
   - Function: `void LogJournalEntry(...)` — Journal entry

9. Create `src/Include/ReversalExit.mqh`:
   - Function: `void MonitorReversals(...)` — Reversal exit monitoring
   - Function: `void CheckReversalExit(...)` — Check for reversal exits

**Commit:** `feat: create all 9 missing EA header files with required functions and types`

### Task 3: Fix Syntax Errors in Main EA File
**Objective:** Resolve all syntax errors and type declarations in VolumeProfile_EA_v1.0.mq5.

**Steps:**
1. Fix type declarations:
   - Add proper type for `currentProfile` (VolumeProfile)
   - Add proper type for `previousSessionProfile` (VolumeProfile)
   - Add proper type for `dailyLimits` (DailyLimitState)
   - Add proper type for `positions` (PositionRecord array)

2. Fix undeclared constants:
   - Define `EA_MAGIC_NUMBER`
   - Define `VOLUME_BINS`
   - Define `HVN_PERCENTILE`
   - Define `LVN_PERCENTILE`
   - Define `VALUE_AREA_PERCENT`
   - Define `LOOKBACK_BARS`
   - Define `SKIP_BAR`
   - Define `TRADING_BLOCKED`
   - Define `SESSION_BLOCKED`
   - Define `LIQUIDITY_REJECTED`
   - Define `SETUP1_SIGNAL_DETECTED`

3. Add missing function declarations at file top

4. **Compile and verify:** Run MT5 compiler on modified EA
   - Expected: 0 errors, 0 warnings
   - Output: `.ex5` binary generated successfully

**Commit:** `fix: resolve all EA syntax errors and declare missing types/constants`

### Task 4: Verify Compiled EA Binary
**Objective:** Confirm .ex5 binary exists and is valid for backtesting.

**Steps:**
1. Check that compiled `.ex5` file exists in MT5's Experts folder
   - Location: `C:\Users\[user]\AppData\Roaming\MetaQuotes\Terminal\[ID]\MQL5\Experts\`
2. Verify file size > 0 and timestamp is recent
3. Open MT5 Strategy Tester and attempt to load the EA
   - Verify: EA appears in Strategy Tester dropdown
   - Verify: EA properties load without errors
4. Create verification report: `src/EA_COMPILATION_VERIFIED.md`
   - Compilation success: YES
   - Binary location: [path]
   - File size: [bytes]
   - Loadable in Strategy Tester: YES
   - Ready for backtesting: YES

**Commit:** `test: verify EA compilation successful and ready for backtesting`

## Success Criteria

- [ ] All 9 header files created in `src/Include/`
- [ ] VolumeProfile_EA_v1.0.mq5 compiles with 0 errors, 0 warnings
- [ ] .ex5 binary generated and loadable in MT5 Strategy Tester
- [ ] EA_COMPILATION_VERIFIED.md confirms readiness
- [ ] Phase 03 backtesting can proceed without compilation blockers

## Must-Haves

- All header files are syntactically valid MQL5
- All function signatures match usage in main EA
- EA compiles to valid .ex5 binary (verified by MT5 IDE)
- No undefined types or functions remain

## Dependencies

- Phase 2 (signal-detection-execution) source code: `src/VolumeProfile_EA_v1.0.mq5`
- MT5 IDE with compiler installed
- Understanding of Phase 2 requirements from REQUIREMENTS.md

## Rollback Plan

If compilation still fails after all headers created:
1. Identify remaining errors from MT5 compiler output
2. Create supplementary gap-closure (02.2) to fix remaining issues
3. Do NOT attempt to backtest with invalid EA

## Unblocks

- Phase 03, Plan 03-02: 2024 Backtest Execution
- Phase 03, Plan 03-03: 2025 Backtest Execution
- Phase 03, Plan 03-04: Metrics Validation and Gate Checks

---

## Notes

This gap-closure is triggered by Phase 03 execution. Once complete, Phase 03 will resume from Plan 03-02 (2024 backtest).
