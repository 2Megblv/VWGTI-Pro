---
status: ready
phase: 02.3
phase_name: signal-detection-execution-gap-closure-mql5-api-fixes
phase_slug: signal-detection-execution-gap-closure-mql5-api-fixes
type: gap_closure
gap_closure: true
autonomous: true
wave: 1
objective: Fix remaining 35 MQL5 API compatibility errors. Code was mixed MQL4/MQL5 — consolidate to pure MQL5 with correct function signatures, enum usage, and CTrade class integration. Result: 0 compilation errors, valid .ex5 binary.
blocker_from: 02.2
blocker_phase: 02.2
blocker_description: Phase 02.2 consolidated headers but exposed MQL5 API incompatibilities. 35 errors remain from MQL4/MQL5 mixing (OnDeinit signature, Send() function, OrdersHistoryTotal, enum conversions, function parentheses).
files_modified:
  - src/VolumeProfile_EA_v1.0.mq5
---

## Objective

Fix all 35 remaining MQL5 API compatibility errors:

1. Correct OnDeinit() function signature
2. Fix CTrade class usage for trade execution
3. Replace MQL4 functions with MQL5 equivalents
4. Fix enum conversions and type mismatches
5. Correct function call syntax
6. Verify: 0 errors, 0 warnings

## Analysis of Errors

**Error Categories (35 total):**

1. **OnDeinit signature** (line 2184) — 1 error
   - Current: `void OnDeinit()` or wrong parameters
   - Fix: `void OnDeinit(const int reason)`

2. **Trade execution (Send function)** (lines 1027, 1492) — 5 errors
   - Current: `Send(request)` — undeclared
   - Fix: `trade.Send(request)` using CTrade instance

3. **MQL4 → MQL5 function replacements** — 8 errors
   - `OrdersHistoryTotal()` → `HistoryOrdersTotal()`
   - `ORDER_NET_PROFIT` → `ORDER_PROFIT`
   - Other deprecated MQL4 functions

4. **Enum type mismatches** (lines 1013, 1484) — 2 errors
   - `cannot convert 0 to enum ENUM_TRADE_REQUEST_ACTIONS`
   - Fix: Use proper enum values or proper type

5. **Function parentheses** (lines 366, 479, 700, 860, 865, etc.) — 15+ errors
   - "open parenthesis expected" on function calls
   - Fix: Add missing parentheses or fix function reference syntax

6. **Type conversions** (lines 234, 1052) — 2 errors
   - `long` to `double`, `ulong` to `long` conversions
   - Fix: Explicit casting or type correction

7. **Reference usage** (line 1274) — 1 error
   - `'&' - reference cannot used` on wrong context
   - Fix: Remove or correct reference operator

8. **Uninitialized variables** (line 181) — 1 warning
   - Variable used before initialization
   - Fix: Initialize before use

## Tasks

### Task 1: Fix OnDeinit Function Signature
**Objective:** Correct MQL5 event handler signature.

**Steps:**
1. Locate `OnDeinit()` function (around line 2184)
2. Change to: `void OnDeinit(const int reason)`
3. If function body uses `reason` parameter, keep it as-is
4. If not used, keep parameter (required by MQL5 standard)
5. Verify no other event handlers have wrong signatures (OnInit, OnTick, OnStart, etc.)

**Commit:** `fix(02.3): correct OnDeinit function signature to MQL5 standard`

### Task 2: Fix CTrade Trade Execution
**Objective:** Properly integrate CTrade class for trade operations.

**Steps:**
1. Verify CTrade class is declared at file top: `CTrade trade;`
2. Find all instances of `Send(request)` — should be `trade.Send(request)`
3. Fix lines ~1027 and ~1492 to use CTrade instance
4. Verify all ENUM_TRADE_REQUEST type initializations are correct:
   - `request.action = TRADE_ACTION_DEAL;` (not 0)
   - `request.action = TRADE_ACTION_PENDING;` for pending orders
5. Verify MqlTradeRequest structure is properly initialized before Send()

**Commit:** `fix(02.3): fix CTrade trade execution and request structure usage`

### Task 3: Replace MQL4 Functions with MQL5 Equivalents
**Objective:** Update all deprecated MQL4 calls to MQL5.

**Steps:**

Replace all instances:
- `OrdersHistoryTotal()` → `HistoryOrdersTotal()`
- `ORDER_NET_PROFIT` → `ORDER_PROFIT` (when used with OrderGetDouble)
- `AccountBalance()` → `AccountInfoDouble(ACCOUNT_BALANCE)`
- `AccountEquity()` → `AccountInfoDouble(ACCOUNT_EQUITY)`
- `AccountFreeMargin()` → `AccountInfoDouble(ACCOUNT_FREEMARGIN)`
- `PositionSelect()` → Use `PositionSelect()` or `PositionGetTicket()` (MQL5 native)
- `OrderSelect()` (MQL4) → `HistoryOrderSelect()` or position lookup (MQL5)

Search for all deprecated function names and replace systematically.

**Commit:** `fix(02.3): replace all MQL4 deprecated functions with MQL5 equivalents`

### Task 4: Fix Enum Type Conversions
**Objective:** Correct ENUM_TRADE_REQUEST_ACTIONS and other enum usage.

**Steps:**
1. Find lines with `cannot convert 0 to enum ENUM_TRADE_REQUEST_ACTIONS` errors
   - Usually: `request.action = 0;`
   - Fix: `request.action = TRADE_ACTION_DEAL;` (or appropriate action)
2. Verify all enum assignments use actual enum values, not integers
3. Check OrderSelect/HistoryOrderSelect calls use correct enum types

**Common fixes:**
```mql5
// WRONG
request.action = 0;

// RIGHT
request.action = TRADE_ACTION_DEAL;  // or TRADE_ACTION_PENDING, etc.
```

**Commit:** `fix(02.3): correct enum type assignments and conversions`

### Task 5: Fix Function Call Parentheses
**Objective:** Correct function call syntax (15+ errors).

**Steps:**
1. Search for "open parenthesis expected" errors
2. Lines affected: 366, 479, 700, 860, 865, 1277, 1279, 1308, 1309, 1479, 1481, 1792, 1798, 1872
3. Common issues:
   - Missing parentheses on function calls: `FunctionName` → `FunctionName()`
   - Missing parameter parentheses: `Array[Size]` with method calls
4. Fix each one systematically

**Example:**
```mql5
// WRONG
double value = SymbolInfoDouble SYMBOL_ASK;

// RIGHT
double value = SymbolInfoDouble(Symbol(), SYMBOL_ASK);
```

**Commit:** `fix(02.3): add missing parentheses to all function calls`

### Task 6: Fix Type Conversions
**Objective:** Handle long/double/ulong implicit conversions.

**Steps:**
1. Find type conversion warnings (lines 234, 1052)
2. Use explicit casting:
   ```mql5
   double value = (double)longVariable;
   long value = (long)ulongVariable;
   ```
3. Or adjust variable types to match assignments

**Commit:** `fix(02.3): add explicit type conversions to eliminate warnings`

### Task 7: Fix Reference Operator Issues
**Objective:** Correct reference usage in function calls.

**Steps:**
1. Line 1274: `'&' - reference cannot used`
   - Check if passing reference where value expected
   - Or removing unnecessary `&` operator
2. Verify function signatures match call usage

**Commit:** `fix(02.3): correct reference operator usage`

### Task 8: Verify Compilation and Create Report
**Objective:** Confirm 0 errors after all fixes.

**Steps:**
1. Save all fixes to `src/VolumeProfile_EA_v1.0.mq5`
2. Open MT5 IDE
3. Click **Tools → Compile**
4. **Expected result:** `0 errors, 0 warnings`
5. Verify `.ex5` file generated
6. Create verification report: `src/EA_MQL5_FIXES_VERIFIED.md`

**Content:**
```markdown
# MQL5 API Fixes Verification Report

**Phase:** 02.3 (Gap Closure — MQL5 API Fixes)

## Fixes Applied

| Issue | Lines | Fix Applied | Status |
|-------|-------|-------------|--------|
| OnDeinit signature | 2184 | Corrected to (const int reason) | ✓ |
| CTrade Send() usage | 1027, 1492 | Changed to trade.Send(request) | ✓ |
| MQL4 function replacements | Multiple | OrdersHistoryTotal → HistoryOrdersTotal | ✓ |
| Enum conversions | 1013, 1484 | Proper ENUM_TRADE_REQUEST_ACTIONS values | ✓ |
| Function parentheses | 14 locations | Added missing () to all calls | ✓ |
| Type conversions | 234, 1052 | Explicit casting applied | ✓ |
| Reference operators | 1274 | Corrected & usage | ✓ |
| Variable initialization | 181 | Initialized before use | ✓ |

## Compilation Result

✅ **0 errors, 0 warnings**

## Phase 03 Readiness

✅ **EA is ready for backtesting**
- Pure MQL5 code (no MQL4 mixing)
- All API calls are valid MQL5
- CTrade class properly integrated
- .ex5 binary generated successfully

## Next Steps

1. Run 2024 backtest (Phase 03-02)
2. Run 2025 backtest (Phase 03-03)
3. Validate metrics (Phase 03-04)
```

**Commit:** `docs(02.3): verify MQL5 fixes and confirm 0 compilation errors`

## Success Criteria

- [ ] OnDeinit signature corrected
- [ ] CTrade trade execution fixed
- [ ] All MQL4 functions replaced with MQL5 equivalents
- [ ] All enum type conversions corrected
- [ ] All function call parentheses fixed
- [ ] Type conversions explicit
- [ ] Reference operators corrected
- [ ] Compilation: 0 errors, 0 warnings
- [ ] .ex5 binary generated and loadable

## Must-Haves

- Pure MQL5 code (no MQL4 APIs)
- All function signatures correct for MQL5
- CTrade class properly integrated
- All enum values valid
- Valid syntax verified by MT5 compiler
- .ex5 binary successfully compiled

## Dependencies

- Phase 02.2 consolidated EA: `src/VolumeProfile_EA_v1.0.mq5`
- MT5 IDE with MQL5 compiler
- Understanding of MQL5 API standards

## Rollback Plan

If new errors appear:
1. Identify problematic section
2. Revert that specific change
3. Use alternative fix approach
4. Re-compile iteratively

## Unblocks

- Phase 03, Plan 03-02: 2024 Backtest Execution
- Phase 03, Plan 03-03: 2025 Backtest Execution
- Phase 03, Plan 03-04: Metrics Validation and Gate Checks

---

## Notes

This phase fixes all remaining MQL5 API compatibility issues. After completion, the EA will compile cleanly with 0 errors and be ready for full backtesting.
